import React from "react";
import { useFetch } from "../../utils/hooks/useFetch";

function Login() 
{
    // const [data, loading, error] = useFetch("/logout")
    // console.log(data, loading, error);
    return (
        <div>
            login
        </div>
    );
}

export default Login;