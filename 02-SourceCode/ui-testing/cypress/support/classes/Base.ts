/**
 * Base class to be extended by other classes
 * @class Base
 * @description Extends the others classes to make functionalities available to all of them
 * @example
 * // Create a new instance of the base class
 * const base = new Base();
 */
class Base
{
    /**
     * Base class constructor
     * @description The base class constructor
     * @public
     * @constructor
     */
    public constructor() {}   
}

export default Base;